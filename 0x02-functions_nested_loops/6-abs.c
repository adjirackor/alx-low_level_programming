#include "main.h"
/**
 * _abs - computes the absolute value of an intege
 * @n: integer to be checked
 *
 * Return: positive integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n;
		return (n);
	}
	else
	{
		return (n);
	}
}

